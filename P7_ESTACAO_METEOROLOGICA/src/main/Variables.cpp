
#include "Variables.h"

/*
    // Pin Definitions
        #define PPD42_SENSOR_PIN A0
        #define LIGHT_SENSOR_PIN A1
        #define UV_SENSOR_PIN A2
        #define RAIN_SENSOR_PIN A3
        
        #define PWM_OUT_PIN 5  // Add a definition for PWM_OUT_PIN
        #define SENSOR_POWER_PIN 6  // Add a definition for SENSOR_POWER_PIN

    //#define debugSerial Serial
        // Replace REPLACE_ME with TTN_FP_EU868 or TTN_FP_US915
        //#define freqPlan TTN_FP_EU868
        
        
    // LoRa Pins
       // #define LORAWAN_TX 3
        //#define LORAWAN_RX 4
        //#define LORAWAN_RST 10
        
    // Sleeping Schedule
        //#define Sleep_Schedule 200  // Remove semicolon here
        DHT20 DHT;

    // Global Variables
    //extern int count;  // Declare count as an external variable
    extern unsigned long startMillis; // Declare time as an external variable
    
    extern char buffer[256];                                                           
    extern float temperatura;
    extern float humidade;
    extern float pressao_atmo;
    extern float chuva;
    extern float qualidade_ar;
    extern float uv;
    extern float luz;*/
